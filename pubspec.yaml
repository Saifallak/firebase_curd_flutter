name: saifoo_crud
description: This is A Proof of concept of using Restful Api with Firebase.
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  http: ^0.12.0+2
  firebase_storage: ^3.0.3
  image_picker: ^0.6.0+17
  uuid: any
  google_maps_flutter: ^0.5.19+2
  geolocator:
  permission_handler:
  tiny_widgets:
    git: https://github.com/TinyProgrammers/tiny_widgets.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
